{
  "name": "arbitrary-equivalence",
  "version": "1.0.5",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "babel-node": "babel-node",
    "test": "jest --watch",
    "compile": "babel --presets es2015 -d lib/ src/ --ignore test.js",
    "prepublish": "npm run compile"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "author": "rxRossi",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
